export const WORLD_WIDTH = 50_000;
export const WORLD_HEIGHT = 50_000;

export const OBJECT_COUNT = 100_000;

export const MIN_OBJECT_SIZE = 8;
export const MAX_OBJECT_SIZE = 64;

export const SPAWN_CELL_SIZE = 110;

export const CHUNK_SIZE = 512;

export const DEBUG_GRID_SPAWN_CELL_INTERVAL = 8;
export const GRID_SIZE = SPAWN_CELL_SIZE * DEBUG_GRID_SPAWN_CELL_INTERVAL;

export const MIN_ZOOM = 0.75;
export const MAX_ZOOM = 2.0;

export const MAX_TEXTURE_DEVICE_PIXEL_RATIO = 4;
